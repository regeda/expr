syntax="proto3";

package ast;

message Node {
  enum Token {
    EXIT = 0;
    CALL = 1;
    STR  = 2;
    INT  = 3;
    BOOL = 4;
    ARR  = 5;
  }

  Token token = 1;
  oneof data {
    string s = 2;
    int64  i = 3;
    bool   b = 4;
  }
  repeated Node nested = 5;
}
